name: Windows Executable Release
on:
  workflow_dispatch:
    inputs:
      formula:
        description: 'Artifact Prefix'
        default: twilio
        required: true
jobs:
  get-tag:
    runs-on: macos-latest
    outputs:
     TAG_NAME: ${{steps.get-tag.outputs.TAG_NAME}}
    steps:
     - uses: actions/checkout@v2
     - name: Getting latest tag
       id: get-tag
       run: |
        git fetch --prune --unshallow
        echo "::set-output name=TAG_NAME::$(git describe --tags $(git rev-list --tags --max-count=1))"
  pack-windows-release:
    runs-on: macos-latest
    steps:
     - uses: actions/checkout@v2
     - run: |
        make install
        brew install makensis
        npx oclif-dev pack:win
     - name: upload artifact
     - uses: actions/upload-artifact@v2
       with:
        name: ${{ github.event.inputs.formula }}-${{ needs.get-tag.outputs.TAG_NAME }}.exe
        path: dist/${{ github.event.inputs.formula }}-${{ needs.get-tag.outputs.TAG_NAME }}.exe

  sign-windows-release:
    runs-on: windows-latest
    steps:
     - name: import certificate
     - run: |
        New-Item -ItemType directory -Path certificate
        Set-Content -Path certificate\certificate.txt -Value '${{ secrets.CERTIFICATE }}'
        certutil -decode certificate\certificate.txt certificate\certificate.pfx
     - name: import Executable
     - uses: actions/download-artifact@v2
       with:
         name: ${{ github.event.inputs.formula }}-${{ needs.get-tag.outputs.TAG_NAME }}.exe
     - name: Code Sign The Executable
     - run: |
          & 'C:/Program Files (x86)/Windows Kits/10/bin/10.0.17763.0/x86/signtool.exe' sign /f certificate\certificate.pfx /p '${{ secrets.CERTIFICATE_PASS }}' /t http://timestamp.digicert.com ${{ github.event.inputs.formula }}-${{ needs.get-tag.outputs.TAG_NAME }}.exe
     - name: Upload binaries to release
       run: node .github/scripts/update-platform-executables.js
       env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        FILE: dist/${{ matrix.artifact_name }}
        ASSET_NAME: ${{ matrix.asset_name }}
        TAG_NAME: ${{ needs.get-tag.outputs.TAG_NAME }}
        REPO_NAME: twilio/twilio-cli

  notify-complete-fail:
    if: ${{ failure() || cancelled() }}
    needs: [windows-executables-release ]
    name: Notify Release Failed
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Slack Notification
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.ALERT_SLACK_WEB_HOOK }}
          SLACK_COLOR: "#ff3333"
          SLACK_USERNAME: CLI Release Bot
          SLACK_ICON_EMOJI: ":ship:"
          SLACK_TITLE: "Twilio Cli"
          SLACK_MESSAGE: 'Windows Executable Release Failed'
          MSG_MINIMAL: actions url
