name: rpm build

on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
  oclifpack:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: create package
        run: | 
            npm cache clear & npm ci
            npm install -g @oclif/dev-cli
            oclif-dev pack --targets=linux-x64
#       - name: upload package
#       - uses: actions/upload-artifact@v2
#         with:
#             name: my-artifact
#             path: dist/twilio-v2.28.1/twilio-v2.28.1-linux-x64.tar.gz
#   rpmbuild:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: Antikythera/build-rpm@latest
#         id: build_rpm
#         with:
#           spec_file: rpm_assets/hello_world.spec
#           sources: |
#             rpm_assets/hello-world-1.0.1.tar.gz
#           variables: |
#             _version=1.0.1
#             _foo=bar
#       - name: build_rpm outputs
#         run: |
#           echo rpm_package_name: ${{ steps.build_rpm.outputs.rpm_package_name }}
#           echo rpm_package_path: ${{ steps.build_rpm.outputs.rpm_package_path }}
